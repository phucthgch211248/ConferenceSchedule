{% extends 'base.html.twig' %}

{% block title %}Edit Conference{% endblock %}

{% block body %}
    <div class="mx-auto w-full max-w-3xl">
        <div class="mb-6">
            <h1 class="text-2xl font-bold text-emerald-950">Edit Conference</h1>
            <p class="text-sm text-emerald-700">Update conference information.</p>
        </div>

        {% for message in app.flashes('error') %}
            <div class="mb-4 rounded-xl border border-rose-200 bg-rose-50 px-4 py-3 text-sm text-rose-700">
                {{ message }}
            </div>
        {% endfor %}

        <form method="post" class="space-y-5 rounded-2xl border border-emerald-200 bg-white p-6 shadow-sm">
            <div>
                <label for="title" class="mb-1 block text-sm font-medium text-emerald-800">Title</label>
                <input id="title" name="title" type="text" value="{{ conference.title }}" required class="w-full rounded-lg border border-emerald-300 px-3 py-2.5 text-sm text-emerald-900 outline-none ring-emerald-600 focus:ring-2">
            </div>

            <div>
                <label for="description" class="mb-1 block text-sm font-medium text-emerald-800">Description</label>
                <textarea id="description" name="description" rows="4" required class="w-full rounded-lg border border-emerald-300 px-3 py-2.5 text-sm text-emerald-900 outline-none ring-emerald-600 focus:ring-2">{{ conference.description }}</textarea>
            </div>

            <div class="grid grid-cols-1 gap-5 sm:grid-cols-2">
                <div>
                    <label for="location" class="mb-1 block text-sm font-medium text-emerald-800">Location</label>
                    <input id="location" name="location" type="text" value="{{ conference.location }}" required class="w-full rounded-lg border border-emerald-300 px-3 py-2.5 text-sm text-emerald-900 outline-none ring-emerald-600 focus:ring-2">
                </div>

                <div>
                    <label for="date" class="mb-1 block text-sm font-medium text-emerald-800">Date</label>
                    <input id="date" name="date" type="date" value="{{ conference.date ? conference.date|date('Y-m-d') : '' }}" required class="w-full rounded-lg border border-emerald-300 px-3 py-2.5 text-sm text-emerald-900 outline-none ring-emerald-600 focus:ring-2">
                </div>
            </div>

            <div class="flex flex-wrap items-center gap-3 pt-2">
                <button type="submit" class="rounded-lg bg-emerald-900 px-4 py-2 text-sm font-semibold text-white transition hover:bg-emerald-800">
                    Update
                </button>
                <a href="{{ path('conference_index') }}" class="rounded-lg border border-emerald-300 px-4 py-2 text-sm font-semibold text-emerald-800 transition hover:bg-emerald-50">
                    Back
                </a>
            </div>
        </form>
    </div>
{% endblock %}
